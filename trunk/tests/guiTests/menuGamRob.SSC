##
##	Tests for menuGamRob
##

guiSetOption( option.name="AutoAddPages", value.string="Off" )
				
methods <- c("MLE + Robust", "Robust", "MLE")
families <- c("Gamma", "Weibull", "LogNormal")

for (method in methods) {
	for (family in families) {
		menuGamRob(dist.family = family, method = method, data = los, print.short.p = T,
			print.long.p = F, plotOverD.p = F, plotQuantile.p = F)

		menuGamRob(dist.family = family, method = method, data = los, print.short.p = F,
			print.long.p = T, plotOverD.p = F, plotQuantile.p = F)

		menuGamRob(dist.family = family, method = method, data = los, print.short.p = T,
			print.long.p = T, plotOverD.p = F, plotQuantile.p = F)

		menuGamRob(dist.family = family, method = method, data = los, print.short.p = F,
			print.long.p = F, plotOverD.p = T, plotQuantile.p = F)

		menuGamRob(dist.family = family, method = method, data = los, print.short.p = F,
			print.long.p = F, plotOverD.p = F, plotQuantile.p = T)

		menuGamRob(dist.family = family, method = method, data = los, print.short.p = F,
			print.long.p = F, plotOverD.p = T, plotQuantile.p = T)
	}
}
